Synchronizing configuration...OK.
Welcome to PICOS on DATAROOM-SW1
admin@DATAROOM-SW1> 
Execute command: show running-config | display set
.
    set alias s as "show|display set|no-more"
    set interface aggregate-ethernet ae10 description "lacp link to SVNE switches"
    set interface aggregate-ethernet ae10 aggregated-ether-options lacp enable true
    set interface aggregate-ethernet ae10 family ethernet-switching port-mode "trunk"
    set interface aggregate-ethernet ae10 family ethernet-switching vlan members 10
    set interface aggregate-ethernet ae10 family ethernet-switching vlan members 20
    set interface aggregate-ethernet ae10 family ethernet-switching vlan members 30
    set interface aggregate-ethernet ae10 family ethernet-switching vlan members 40
    set interface aggregate-ethernet ae10 family ethernet-switching vlan members 300
    set interface gigabit-ethernet te-1/1/1 ether-options 802.3ad "ae10"
    set interface gigabit-ethernet te-1/1/2 ether-options 802.3ad "ae10"
    set interface gigabit-ethernet te-1/1/11 family ethernet-switching native-vlan-id 300
    set interface gigabit-ethernet te-1/1/11 speed "auto"
    set interface gigabit-ethernet te-1/1/12 family ethernet-switching native-vlan-id 20
    set interface gigabit-ethernet te-1/1/13 family ethernet-switching native-vlan-id 30
    set interface gigabit-ethernet te-1/1/14 family ethernet-switching native-vlan-id 40
    set interface gigabit-ethernet te-1/1/15 family ethernet-switching native-vlan-id 300
    set interface gigabit-ethernet te-1/1/27 family ethernet-switching native-vlan-id 300
    set protocols igmp-snooping enable true
    set protocols igmp-snooping traceoptions flag all
    set protocols igmp-snooping vlan-id 300 enable true
    set protocols igmp-snooping vlan-id 300 mrouter interface ae10
    set protocols lldp enable true
    set protocols spanning-tree force-version 4
    set protocols spanning-tree pvst vlan 300 enable true
    set protocols sflow agent-id 10.88.88.44
    set protocols sflow disable false
    set protocols sflow sampling-rate ingress 1000
    set protocols sflow sampling-rate egress 1000
    set protocols sflow polling-interval 10
    set protocols sflow header-len 128
    set protocols sflow source-address 10.88.88.44
    set protocols sflow collector 10.88.88.20
    set protocols snmp community public
    set protocols snmp contact "support@pica8.com"
    set protocols snmp location "Palo-Alto"
    set protocols snmp trap-group targets 10.88.88.20 security-name public
    set protocols static route 0.0.0.0/0 next-hop 192.168.42.119
    set system management-ethernet eth0 ip-address IPv4 "10.88.88.44/24"
    set system hostname "DATAROOM-SW1"
    set vlans vlan-id 10
    set vlans vlan-id 20
    set vlans vlan-id 30
    set vlans vlan-id 40
    set vlans vlan-id 100
    set vlans vlan-id 300

admin@DATAROOM-SW1> 